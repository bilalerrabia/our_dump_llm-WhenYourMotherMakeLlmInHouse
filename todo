test callme with gpt-2.
optimasion callme.
run callme with ourdump llm.

use the position embedding from attention is all you need.
data_test to dir/ not just one file.
        -> embedding hold the meaning about the words, but the attention hold the relations between the words.
        -> attention score calculate by the dot prodect.
        -> Attention weights = softmax(attention scores).
        -> context vectore(qi) = somme(for token in tokens (attention score(qi, xi) * embedding(xi)))
search about the torch implimentation of softmax.
qkv_bias.
type hints.
make lint-strict.
